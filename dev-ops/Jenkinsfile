#!/usr/bin/env groovy

pipeline {
    agent {
        docker {
          label 'dockerserver'
          image 'node:14-alpine'
        }
    }
    stages {
        stage('Test') {
            steps {
                sh 'node --version'
            }
        }
    }
}

