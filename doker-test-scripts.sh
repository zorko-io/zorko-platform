# Temp solution to check containers

# Build backend with context
docker build -t backend-1 -f dev-ops/backend/Dockerfile .

# TODO: run with custom ports, volumes and commands

# TODO: build and run with nginx
