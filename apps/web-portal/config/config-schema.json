{
  "http": ["required", {"nested_object": {
    "port": ["required", "positive_integer"]
  }}],
  "logger": ["required", {"nested_object": {
    "isPrettyPrint": ["required", "boolean"]
  }}],
  "auth": ["required", {"nested_object": {
    "secret": ["required", "string"],
    "expiresIn": ["required", "positive_integer"]
  }}]
}
